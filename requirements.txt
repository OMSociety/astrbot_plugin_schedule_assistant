# Schedule Assistant 插件依赖
# 核心依赖
aiohttp>=3.8.0
apscheduler>=3.10.0
python-dateutil>=2.8.0

# 可选依赖（用于特定功能）
# markdownify>=0.11.0  # markdown.py 纯文本降级（strip_markdown），缺失时自动回退正则粗剥
# markdown-it-py>=3.0.0  # 同上，与 markdownify 配套使用
# requests>=2.28.0  # 用于HTTP请求（已禁用，使用aiohttp代替）
# beautifulsoup4>=4.11.0  # 用于HTML解析
# lxml>=4.9.0  # 用于XML解析

# 开发依赖（可选）
# pytest>=7.0.0
# pytest-asyncio>=0.21.0
# mypy>=1.0.0
# ruff>=0.1.0
